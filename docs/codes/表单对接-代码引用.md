# 表单对接（代码引用与片段）

本页用于 VitePress 发布：所有代码引用均使用相对路径，并直接贴出关键行内容（带行号范围）。

## 1. PropertiesPanel：form slot 覆盖默认 ElementForm

源码链接：[`package/penal/PropertiesPanel.vue`](../../package/penal/PropertiesPanel.vue)

关键片段（表单折叠项渲染逻辑）：[`PropertiesPanel.vue#L16-L22`](../../package/penal/PropertiesPanel.vue#L16-L22)

```vue
16  <el-collapse-item name="condition" v-if="formVisible" key="form">
17    <div slot="title" class="panel-tab__title"><i class="el-icon-s-order"></i>表单</div>
18    <template v-if="$scopedSlots.form || $slots.form">
19      <slot name="form" v-bind="{ id: elementId, elementId, type: elementType, businessObject: elementBusinessObject, element: bpmnElement }" />
20    </template>
21    <element-form v-else :id="elementId" :type="elementType" />
22  </el-collapse-item>
```

补充：表单面板展示范围（StartEvent / UserTask）：[`PropertiesPanel.vue#L201`](../../package/penal/PropertiesPanel.vue#L201)

```js
201  this.formVisible = this.elementType === "UserTask" || this.elementType === "StartEvent";
```

## 2. PropertiesPanel：向 window 注入 bpmnInstances（自定义面板读写依赖）

源码链接：[`package/penal/PropertiesPanel.vue`](../../package/penal/PropertiesPanel.vue)

关键片段（初始化注入）：[`PropertiesPanel.vue#L142-L161`](../../package/penal/PropertiesPanel.vue#L142-L161)

```js
142  initModels() {
143    // 初始化 modeler 以及其他 moddle
144    if (!this.bpmnModeler) {
145      // 避免加载时 流程图 并未加载完成
146      this.timer = setTimeout(() => this.initModels(), 10);
147      return;
148    }
149    if (this.timer) clearTimeout(this.timer);
150    window.bpmnInstances = {
151      modeler: this.bpmnModeler,
152      modeling: this.bpmnModeler.get("modeling"),
153      moddle: this.bpmnModeler.get("moddle"),
154      eventBus: this.bpmnModeler.get("eventBus"),
155      bpmnFactory: this.bpmnModeler.get("bpmnFactory"),
156      elementFactory: this.bpmnModeler.get("elementFactory"),
157      elementRegistry: this.bpmnModeler.get("elementRegistry"),
158      replace: this.bpmnModeler.get("replace"),
159      selection: this.bpmnModeler.get("selection")
160    };
161    this.getActiveElement();
```

## 3. ElementForm：formKey（表单 ID）读取与写回

源码链接：[`package/penal/form/ElementForm.vue`](../../package/penal/form/ElementForm.vue)

关键片段（读取 formKey、初始化/创建 extensionElements 与 FormData）：[`ElementForm.vue#L196-L218`](../../package/penal/form/ElementForm.vue#L196-L218)

```js
196  resetFormList() {
197    this.bpmnElement = window.bpmnInstances.bpmnElement;
198    this.formKey = this.bpmnElement.businessObject.formKey;
199    // 获取元素扩展属性 或者 创建扩展属性
200    this.elExtensionElements =
201      this.bpmnElement.businessObject.get("extensionElements") || window.bpmnInstances.moddle.create("bpmn:ExtensionElements", { values: [] });
202    // 获取元素表单配置 或者 创建新的表单配置
203    this.formData =
204      this.elExtensionElements.values.filter(ex => ex.$type === `${this.prefix}:FormData`)?.[0] ||
205      window.bpmnInstances.moddle.create(`${this.prefix}:FormData`, { fields: [] });
206
207    // 业务标识 businessKey， 绑定在 formData 中
208    this.businessKey = this.formData.businessKey;
209
210    // 保留剩余扩展元素，便于后面更新该元素对应属性
211    this.otherExtensions = this.elExtensionElements.values.filter(ex => ex.$type !== `${this.prefix}:FormData`);
212
213    // 复制原始值，填充表格
214    this.fieldList = JSON.parse(JSON.stringify(this.formData.fields || []));
215
216    // 更新元素扩展属性，避免后续报错
217    this.updateElementExtensions();
218  },
```

关键片段（写回 formKey）：[`ElementForm.vue#L219-L221`](../../package/penal/form/ElementForm.vue#L219-L221)

```js
219  updateElementFormKey() {
220    window.bpmnInstances.modeling.updateProperties(this.bpmnElement, { formKey: this.formKey });
221  },
```

## 4. ElementForm：businessKey 写回（存储在 FormData.businessKey）

源码链接：[`package/penal/form/ElementForm.vue`](../../package/penal/form/ElementForm.vue)

关键片段：[`ElementForm.vue#L222-L226`](../../package/penal/form/ElementForm.vue#L222-L226)

```js
222  updateElementBusinessKey() {
223    window.bpmnInstances.modeling.updateModdleProperties(this.bpmnElement, this.formData, {
224      businessKey: this.businessKey
225    });
226  },
```

## 5. ElementForm：FormField / Validation / Properties / Value 的对象创建与写回

源码链接：[`package/penal/form/ElementForm.vue`](../../package/penal/form/ElementForm.vue)

关键片段（创建字段、校验、属性、枚举值，并写入 formData.fields）：[`ElementForm.vue#L295-L334`](../../package/penal/form/ElementForm.vue#L295-L334)

```js
295  saveField() {
296    const { id, type, label, defaultValue, datePattern } = this.formFieldForm;
297    const Field = window.bpmnInstances.moddle.create(`${this.prefix}:FormField`, { id, type, label });
298    defaultValue && (Field.defaultValue = defaultValue);
299    datePattern && (Field.datePattern = datePattern);
300    // 构建属性
301    if (this.fieldPropertiesList?.length) {
302      const fieldPropertyList = this.fieldPropertiesList.map(fp => {
303        return window.bpmnInstances.moddle.create(`${this.prefix}:Property`, { id: fp.id, value: fp.value });
304      });
305      Field.properties = window.bpmnInstances.moddle.create(`${this.prefix}:Properties`, {
306        values: fieldPropertyList
307      });
308    }
309    // 构建校验规则
310    if (this.fieldConstraintsList?.length) {
311      const fieldConstraintList = this.fieldConstraintsList.map(fc => {
312        return window.bpmnInstances.moddle.create(`${this.prefix}:Constraint`, { name: fc.name, config: fc.config });
313      });
314      Field.validation = window.bpmnInstances.moddle.create(`${this.prefix}:Validation`, {
315        constraints: fieldConstraintList
316      });
317    }
318    // 构建枚举值
319    if (this.fieldEnumList?.length) {
320      Field.values = this.fieldEnumList.map(fe => {
321        return window.bpmnInstances.moddle.create(`${this.prefix}:Value`, { name: fe.name, id: fe.id });
322      });
323    }
324    // 更新数组 与 表单配置实例
325    if (this.formFieldIndex === -1) {
326      this.fieldList.push(this.formFieldForm);
327      this.formData.fields.push(Field);
328    } else {
329      this.fieldList.splice(this.formFieldIndex, 1, this.formFieldForm);
330      this.formData.fields.splice(this.formFieldIndex, 1, Field);
331    }
332    this.updateElementExtensions();
333    this.fieldModelVisible = false;
334  },
```

关键片段（写回 extensionElements，并保留其它扩展元素）：[`ElementForm.vue#L355-L364`](../../package/penal/form/ElementForm.vue#L355-L364)

```js
355  updateElementExtensions() {
356    // 更新回扩展元素
357    const newElExtensionElements = window.bpmnInstances.moddle.create(`bpmn:ExtensionElements`, {
358      values: this.otherExtensions.concat(this.formData)
359    });
360    // 更新到元素上
361    window.bpmnInstances.modeling.updateProperties(this.bpmnElement, {
362      extensionElements: newElExtensionElements
363    });
364  }
```

## 6. Descriptor：FormSupported.formKey（决定 formKey 可序列化为 XML 属性）

### 6.1 Camunda descriptor

源码链接：[`package/designer/plugins/descriptor/camundaDescriptor.json`](../../package/designer/plugins/descriptor/camundaDescriptor.json)

关键片段：[`camundaDescriptor.json#L198-L215`](../../package/designer/plugins/descriptor/camundaDescriptor.json#L198-L215)

```json
198    {
199      "name": "FormSupported",
200      "isAbstract": true,
201      "extends": [
202        "bpmn:StartEvent",
203        "bpmn:UserTask"
204      ],
205      "properties": [
206        {
207          "name": "formHandlerClass",
208          "isAttr": true,
209          "type": "String"
210        },
211        {
212          "name": "formKey",
213          "isAttr": true,
214          "type": "String"
215        }
```

### 6.2 Activiti descriptor

源码链接：[`package/designer/plugins/descriptor/activitiDescriptor.json`](../../package/designer/plugins/descriptor/activitiDescriptor.json)

关键片段：[`activitiDescriptor.json#L197-L214`](../../package/designer/plugins/descriptor/activitiDescriptor.json#L197-L214)

```json
197    {
198      "name": "FormSupported",
199      "isAbstract": true,
200      "extends": [
201        "bpmn:StartEvent",
202        "bpmn:UserTask"
203      ],
204      "properties": [
205        {
206          "name": "formHandlerClass",
207          "isAttr": true,
208          "type": "String"
209        },
210        {
211          "name": "formKey",
212          "isAttr": true,
213          "type": "String"
214        }
```

## 7. Descriptor：FormData / FormField / Validation / Constraint / Properties / Property / Value

源码链接：[`package/designer/plugins/descriptor/camundaDescriptor.json`](../../package/designer/plugins/descriptor/camundaDescriptor.json)

### 7.1 Properties / Property

片段：[`camundaDescriptor.json#L524-L560`](../../package/designer/plugins/descriptor/camundaDescriptor.json#L524-L560)

```json
524    {
525      "name": "Properties",
526      "superClass": [
527        "Element"
528      ],
529      "meta": {
530        "allowedIn": [ "*" ]
531      },
532      "properties": [
533        {
534          "name": "values",
535          "type": "Property",
536          "isMany": true
537        }
538      ]
539    },
540    {
541      "name": "Property",
542      "superClass": [
543        "Element"
544      ],
545      "properties": [
546        {
547          "name": "id",
548          "type": "String",
549          "isAttr": true
550        },
551        {
552          "name": "name",
553          "type": "String",
554          "isAttr": true
555        },
556        {
557          "name": "value",
558          "type": "String",
559          "isAttr": true
560        }
```

### 7.2 Value

片段：[`camundaDescriptor.json#L680-L700`](../../package/designer/plugins/descriptor/camundaDescriptor.json#L680-L700)

```json
680    {
681      "name": "Value",
682      "superClass": [
683        "InputOutputParameterDefinition"
684      ],
685      "properties": [
686        {
687          "name": "id",
688          "isAttr": true,
689          "type": "String"
690        },
691        {
692          "name": "name",
693          "isAttr": true,
694          "type": "String"
695        },
696        {
697          "name": "value",
698          "isBody": true,
699          "type": "String"
700        }
```

### 7.3 FormData / FormField / Validation / Constraint

片段：[`camundaDescriptor.json#L974-L1064`](../../package/designer/plugins/descriptor/camundaDescriptor.json#L974-L1064)

```json
974    {
975      "name": "FormData",
976      "superClass": [ "Element" ],
977      "meta": {
978        "allowedIn": [
979          "bpmn:StartEvent",
980          "bpmn:UserTask"
981        ]
982      },
983      "properties": [
984        {
985          "name": "fields",
986          "type": "FormField",
987          "isMany": true
988        },
989        {
990          "name": "businessKey",
991          "type": "String",
992          "isAttr": true
993        }
994      ]
995    },
996    {
997      "name": "FormField",
998      "superClass": [ "Element" ],
999      "properties": [
1000        {
1001          "name": "id",
1002          "type": "String",
1003          "isAttr": true
1004        },
1005        {
1006          "name": "label",
1007          "type": "String",
1008          "isAttr": true
1009        },
1010        {
1011          "name": "type",
1012          "type": "String",
1013          "isAttr": true
1014        },
1015        {
1016          "name": "datePattern",
1017          "type": "String",
1018          "isAttr": true
1019        },
1020        {
1021          "name": "defaultValue",
1022          "type": "String",
1023          "isAttr": true
1024        },
1025        {
1026          "name": "properties",
1027          "type": "Properties"
1028        },
1029        {
1030          "name": "validation",
1031          "type": "Validation"
1032        },
1033        {
1034          "name": "values",
1035          "type": "Value",
1036          "isMany": true
1037        }
1038      ]
1039    },
1040    {
1041      "name": "Validation",
1042      "superClass": [ "Element" ],
1043      "properties": [
1044        {
1045          "name": "constraints",
1046          "type": "Constraint",
1047          "isMany": true
1048        }
1049      ]
1050    },
1051    {
1052      "name": "Constraint",
1053      "superClass": [ "Element" ],
1054      "properties": [
1055        {
1056          "name": "name",
1057          "type": "String",
1058          "isAttr": true
1059        },
1060        {
1061          "name": "config",
1062          "type": "String",
1063          "isAttr": true
1064        }
```

## 8. 封装组件透传 form slot（避免误伤默认 ElementForm）

当你在封装组件内部使用 `<my-properties-panel>` 时，如果无条件写了 `v-slot:form`，即使上层没有传任何 slot 内容，`<my-properties-panel>` 也会认为 “form slot 存在”，从而隐藏默认的 `<element-form />`。

推荐写法：仅当封装组件确实收到了 `form` slot 时才透传。

```vue
<my-properties-panel ...>
  <template v-if="$scopedSlots.form" v-slot:form="data">
    <slot name="form" v-bind="data" />
  </template>
</my-properties-panel>
```

同理，如果你也透传了 `business` slot，建议也加 `v-if="$scopedSlots.business"`，避免业务面板出现“空白 tab”。
